<div class="col-md-12">
<div class="text-center col-md-12 page-header"></br>
  <div class="col-md-2">
    <%= image_tag "heart.png",width: "50%"%>
  </div>
  <h2 class="text-center col-md-8">Registro de Ayudas Sociales</br>
     <small>Fundación Social Nueva Esparta</small></br>

    <%#if current_user.has_role? :discapacidad%>
       <!--<small>Coord. de Discapacidad</small></br>-->
    <%#elsif current_user.has_role? :niños%>
       <!--<small>Coord. de Niños, Niñas y Adolescentes</small></br>-->
    <%#elsif current_user.has_role? :adulto%>
       <!--<small>Coord. de Adultos y Adultos Mayores</small></br>-->
    <%#end%>
  </h2>
</div>

  <div class="col-md-12" id="solicitantes">
    <%=render "solicitantes"%>
  </div>
</div>


<script>
             var classes = ".resultado";
             var delay = (function () {
               var timer = 0;
               return function (callback, ms) {
                 clearTimeout(timer);
                 timer = setTimeout(callback, ms);
               };
             })();
             function loading() {
               $("#search_icon").attr("class", "fa fa-refresh fa-spin fa-fw")
             }
             function query() {
               loading();
               return "/solicitantes?search=" + $("#search").val();
             }
             $(function () {

               $("#search").keyup(function () {
                 delay(function () {
                   $.getScript(query());
                   return false;
                 }, 500);
               });

             });
</script>
